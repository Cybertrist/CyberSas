# Secrets, clés, certificats et comptes : propres à chaque installation.
etat/
.env
# Signée avec la clé du verrou de chaque installation.
politique/politique.sig

# Pages intermédiaires des figures : elles portent des chemins de la machine.
docs/tools/html/

# Le moteur compilé : scripts/moteur.sh le refait.
mobile/android/app/libs/
docs/tools/controle/
